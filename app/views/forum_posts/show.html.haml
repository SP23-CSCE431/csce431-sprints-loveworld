%h1= @forum_post.title
%p= @forum_post.content

= link_to "Edit", edit_forum_post_path(@forum_post), class: "button"
= link_to "Delete", forum_post_path(@forum_post), method: :delete, data: { confirm: "Are you sure you want to do this ?" }, class: "button"
= link_to "Home", root_path